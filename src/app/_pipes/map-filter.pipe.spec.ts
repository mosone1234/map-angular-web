import { MapFilterPipe } from './map-filter.pipe';

describe('MapFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MapFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
